# AI Skills Match Engine

A deterministic, explainable, and type-safe Resume<->JD matching engine.

## Structure

- `backend/`: FastAPI + Pydantic Application
- `frontend/`: React + Vite Application

## Getting Started

### Backend
1. `cd backend`
2. `pip install -e .`
3. `uvicorn src.main:app --reload`
